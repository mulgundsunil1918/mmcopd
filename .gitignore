node_modules/
out/
.vite/
dist/
dist-demo/
dist-installer/
*.log
.env
.env.local
.claude/


# Licensing — vendor private key must never be committed
license-keys/

# activation server local code store (never commit)
server/data/

# Vendor licence admin — LOCAL ONLY, never push (private tool + customer data + machine IDs + codes)
tools/keygen-server.mjs
tools/keygen-setup.mjs
tools/.keygen-data/
tools/keygen.enc
tools/keygen-vault.json
keygen-portable/
.cache/
